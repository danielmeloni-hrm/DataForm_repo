config {
    type: "table",
    schema: "dataform",
    name: "click_events_latest",
    description: "Eventi filtrati con prefisso click_ dall'ultima partizione disponibile"
}

SELECT
  *
FROM
  ${ref("raw_events_cleaned")}
WHERE
  event_name LIKE 'click_%'
  